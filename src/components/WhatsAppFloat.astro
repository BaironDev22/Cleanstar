---
// WhatsApp Float Component para Cleanstar Detailing
---

<div class="whatsapp-float">
  <a
    href="https://wa.me/56947346095?text=Hola%20Cleanstar%20Detailing,%20me%20interesa%20conocer%20más%20sobre%20sus%20servicios%20de%20detailing%20automotriz"
    target="_blank"
    rel="noopener noreferrer"
    class="whatsapp-button w-14 h-14 bg-green-500/90 hover:bg-green-500 rounded-full shadow-lg hover:shadow-xl transition-all duration-500 flex items-center justify-center group backdrop-blur-sm border border-green-400/20"
    aria-label="Contáctanos por WhatsApp - Cleanstar Detailing"
  >
    <svg 
      class="w-7 h-7 text-white group-hover:scale-110 transition-transform duration-300" 
      fill="currentColor" 
      viewBox="0 0 24 24"
    >
      <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893A11.821 11.821 0 0020.885 3.785"/>
    </svg>
  </a>
  
  <!-- Tooltip especializado para Cleanstar -->
  <div class="whatsapp-tooltip absolute bottom-full right-0 mb-3 opacity-0 group-hover:opacity-100 transition-all duration-300 pointer-events-none transform translate-y-1 group-hover:translate-y-0">
    <div class="bg-gray-900/95 backdrop-blur-sm text-white text-xs px-4 py-3 rounded-lg whitespace-nowrap shadow-xl border border-green-400/30">
      <div class="flex items-center gap-2">
        <span>🚗</span>
        <span class="font-medium">¿Necesitas cotizar detailing?</span>
      </div>
      <div class="text-green-300 text-[10px] mt-1">
        Respuesta inmediata • Villa Alemana
      </div>
      <div class="absolute top-full right-4 w-0 h-0 border-l-[6px] border-r-[6px] border-t-[6px] border-transparent border-t-gray-900/95"></div>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const whatsappButton = document.querySelector('.whatsapp-float') as HTMLElement;
    
    if (whatsappButton) {
      let hasAnimated = false;
      let hasInteracted = false;
      
      // Detect if device is mobile
      const isMobile = window.innerWidth <= 768 || 'ontouchstart' in window;
      
      // Reduce animation delay on mobile for better UX
      const animationDelay = isMobile ? 15000 : 20000; // 15s on mobile, 20s on desktop
      
      // Gentle attention animation after user has been on page
      setTimeout(() => {
        if (!hasAnimated && !hasInteracted) {
          whatsappButton.style.animation = 'cleanstar-pulse 1.2s ease-in-out';
          hasAnimated = true;
          
          // Remove animation after it completes
          setTimeout(() => {
            whatsappButton.style.animation = '';
          }, 1200);
        }
      }, animationDelay);
      
      // Mark as interacted when user hovers or touches
      whatsappButton.addEventListener('mouseenter', () => {
        hasInteracted = true;
      });
      
      // Touch interaction for mobile
      whatsappButton.addEventListener('touchstart', () => {
        hasInteracted = true;
      });
      
      // Track scroll interaction with different threshold for mobile
      let scrollThreshold = false;
      const scrollLimit = isMobile ? 300 : 500; // Lower threshold on mobile
      
      window.addEventListener('scroll', () => {
        if (window.scrollY > scrollLimit && !scrollThreshold) {
          scrollThreshold = true;
          hasInteracted = true;
        }
      });
      
      // Add click tracking for analytics
      whatsappButton.addEventListener('click', () => {
        console.log('WhatsApp clicked - Cleanstar Detailing');
        // Optional: Add analytics tracking here
      });
      
      // Handle orientation change on mobile
      if (isMobile) {
        window.addEventListener('orientationchange', () => {
          setTimeout(() => {
            // Recalculate position if needed after orientation change
            whatsappButton.style.opacity = '0.9';
          }, 100);
        });
      }
    }
  });
</script>

<style>
  .whatsapp-float {
    position: fixed;
    bottom: 24px;
    right: 24px;
    z-index: 50;
    opacity: 0.9;
    transition: opacity 0.3s ease, transform 0.3s ease;
  }
  
  .whatsapp-float:hover {
    opacity: 1;
    transform: translateY(-2px);
  }
  
  .whatsapp-button {
    filter: drop-shadow(0 4px 12px rgba(34, 197, 94, 0.3));
  }
  
  .whatsapp-button:hover {
    filter: drop-shadow(0 6px 20px rgba(34, 197, 94, 0.4));
  }
  
  /* Custom pulse animation for Cleanstar */
  @keyframes cleanstar-pulse {
    0%, 100% { 
      transform: scale(1);
      opacity: 0.9;
    }
    25% {
      transform: scale(1.05);
      opacity: 1;
    }
    50% { 
      transform: scale(1.1);
      opacity: 1;
    }
    75% {
      transform: scale(1.05);
      opacity: 1;
    }
  }
  
  /* Responsive adjustments */
  @media (max-width: 768px) {
    .whatsapp-float {
      bottom: 24px;
      right: 20px;
    }
    
    /* Ensure tooltip stays visible on tablets */
    .whatsapp-tooltip {
      right: -4px;
    }
  }
  
  @media (max-width: 480px) {
    .whatsapp-float {
      bottom: 20px;
      right: 16px;
    }
    
    .whatsapp-button {
      width: 56px;
      height: 56px;
    }
    
    .whatsapp-button svg {
      width: 26px;
      height: 26px;
    }
    
    .whatsapp-tooltip {
      right: -12px;
      bottom: calc(100% + 12px);
      max-width: 280px;
    }
    
    .whatsapp-tooltip > div {
      padding: 10px 12px;
      font-size: 12px;
    }
    
    .whatsapp-tooltip .text-[10px] {
      font-size: 10px !important;
    }
  }
  
  /* Better positioning for very small screens */
  @media (max-width: 360px) {
    .whatsapp-float {
      bottom: 16px;
      right: 12px;
    }
    
    .whatsapp-button {
      width: 52px;
      height: 52px;
    }
    
    .whatsapp-button svg {
      width: 24px;
      height: 24px;
    }
    
    .whatsapp-tooltip {
      right: -8px;
      bottom: calc(100% + 8px);
      max-width: 250px;
    }
    
    .whatsapp-tooltip > div {
      padding: 8px 10px;
      font-size: 11px;
    }
  }
  
  /* Landscape phones - prevent obstruction */
  @media (max-width: 640px) and (max-height: 400px) and (orientation: landscape) {
    .whatsapp-float {
      bottom: 12px;
      right: 12px;
    }
    
    .whatsapp-button {
      width: 48px;
      height: 48px;
    }
    
    .whatsapp-button svg {
      width: 22px;
      height: 22px;
    }
    
    /* Hide tooltip in landscape to save space */
    .whatsapp-tooltip {
      display: none;
    }
  }
  
  /* Ensure it doesn't interfere with cookie banners or other floating elements */
  .whatsapp-float {
    margin-bottom: env(safe-area-inset-bottom, 0);
  }
</style>
